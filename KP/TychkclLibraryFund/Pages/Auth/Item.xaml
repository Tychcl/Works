<UserControl x:Class="TychkclLibraryFund.Pages.Auth.Item"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:TychkclLibraryFund.Pages.Auth"
             mc:Ignorable="d" Style="{StaticResource USStyle}"
             Margin="3" >
    <Grid Margin="5" >
        <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
            <TextBox Style="{StaticResource TextBox}" x:Name="name" IsEnabled="False"/>
            <Label Style="{StaticResource ItemTextLabel}" x:Name="lab"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="delete" Click="del" ToolTip="Удалить" fa:Awesome.Content="Trash" Style="{StaticResource ItemButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
