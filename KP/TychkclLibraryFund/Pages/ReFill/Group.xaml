<UserControl x:Class="TychkclLibraryFund.Pages.ReFill.Group"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TychkclLibraryFund.Pages.ReFill"
             mc:Ignorable="d" 
             Style="{StaticResource USStyle}"
             Margin="3" Background="{StaticResource LightBack}">
    <StackPanel Margin="5" Unloaded="Page_Unloaded">
        <Grid>
            <Label x:Name="text" Style="{StaticResource ItemTextLabel}" HorizontalAlignment="Left"/>
            <Button x:Name="arrow" fa:Awesome.Content="ArrowsV" HorizontalAlignment="Right" Style="{StaticResource Button}" Width="25" Height="25" Click="click"/>
        </Grid>
        <StackPanel x:Name="parent" Visibility="Collapsed"/>
    </StackPanel>
</UserControl>
